# ComfyUI-MinuteSaver
#
# 无强制第三方依赖：
#  - 图像保存使用 Pillow（numpy / Pillow 为 ComfyUI 自带）
#  - 视频保存使用 ffmpeg 可执行文件；未安装时自动回退使用
#    ComfyUI 自带的 imageio-ffmpeg 内置 ffmpeg（videohelpersuite 依赖）
#
# 如需自己指定 ffmpeg，可设置环境变量 FFMPEG_BINARY 指向 ffmpeg.exe
